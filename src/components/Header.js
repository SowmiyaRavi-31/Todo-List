import React from "react";
import "./Header.css";

const Header = () => (
  <header className="header">
    <h1 className="header-title">Animated To-Do List</h1>
    <p className="header-subtitle">Manage your tasks with style!</p>
  </header>
);

export default Header;
